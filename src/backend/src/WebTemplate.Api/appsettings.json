{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "_AllowedHosts_comment": "預設放寬為 * 以避免 downstream 首次部署時因 Host header 不符被 Kestrel Host Filtering Middleware 直接 400（wall-of-text warning，極難 debug）。appsettings.Production.json 會覆寫為具體網域白名單（例如 api.example.com），強迫實際 prod 部署時需明示設定。",
  "AllowedHosts": "*",
  "Jwt": {
    "Issuer": "WebTemplate",
    "Audience": "WebTemplate",
    "AccessTokenExpirationMinutes": 15,
    "RefreshTokenExpirationDays": 7
  }
}
